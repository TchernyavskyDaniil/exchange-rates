const DEFAULT_DATE_OPTIONS = {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
}

const FROM_CURRENCY = 'USD'

const TO_CURRENCY = 'RUB'

export default {
  DEFAULT_DATE_OPTIONS,
  FROM_CURRENCY,
  TO_CURRENCY,
}
